# Lightweight requirements for Vercel deployment
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
google-generativeai==0.3.2
requests==2.31.0
Pillow==10.1.0
gunicorn==21.2.0

# Note: opencv-python-headless and pytesseract are too large for Vercel
# Vercel does NOT support Tesseract. Deploy backend to Railway/Render instead.
